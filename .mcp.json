{
  "mcpServers": {
    "countries": {
      "command": "./mvnw",
      "args": [
        "-pl",
        "countries-mcp-server",
        "spring-boot:run"
      ],
      "env": {}
    },
    "weather": {
      "command": "mcp-weather",
      "args": [],
      "env": {
        "WEATHER_API_KEY": "${WEATHER_API_KEY}"
      }
    }
  }
}
