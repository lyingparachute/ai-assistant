# Base URL of the Assistant API. Configured source for the chat UI; when unset the UI
# falls back to DEFAULT_API_URL (http://localhost:8080) in src/lib/api.ts.
PUBLIC_ASSISTANT_API_URL=http://localhost:8080
